/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "images.unsplash.com",
            "sympli-blog-content.s3.amazonaws.com",
            "iexperto.io"
        ]
    }
}

module.exports = nextConfig
