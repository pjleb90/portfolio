/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "img.freepik.com",
            "www.goodcore.co.uk";
        ]
    }
}

module.exports = nextConfig
